spring:
  datasource:
    url: jdbc:mysql://localhost/hotelBookingRoom?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  servlet:
    context-path: /api
  port: 9090




